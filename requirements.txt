# Django core
Django>=5.1,<5.2
djangorestframework>=3.15,<4.0

# Password hashing
bcrypt>=4.1,<5.0

# Environment variables
python-decouple>=3.8,<4.0

# CORS headers (for frontend integration - kept for potential external APIs)
django-cors-headers>=4.3,<5.0

# Razorpay payment gateway
razorpay>=1.4,<2.0

# Admin UI
django-unfold>=0.90,<1.0

# Image processing (required for ImageField)
Pillow>=12.0,<13.0

# Packaging (Required for razorpay/pkg_resources interactions in newer Python versions)
setuptools<70.0

# Added for production / deployment / background tasks
django-q2>=1.7,<2.0
psycopg2-binary>=2.9,<3.0
dj-database-url>=2.1,<3.0
whitenoise>=6.6,<7.0
django-allauth>=65.0,<66.0
PyJWT>=2.8.0
django-redis>=5.4.0
